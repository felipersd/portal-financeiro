{
  "name": "portal-financeiro-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "nx run-many --target=build",
    "test": "nx run-many --target=test",
    "lint": "nx run-many --target=lint"
  },
  "devDependencies": {
    "nx": "^20.0.0"
  }
}
